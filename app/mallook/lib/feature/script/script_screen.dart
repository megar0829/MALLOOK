import 'package:flutter/material.dart';

class ScriptScreen extends StatelessWidget {
  final int scriptId;

  const ScriptScreen({super.key, required this.scriptId});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
