FROM amazoncorretto:17

ARG JAR_FILE=build/libs/mallook-0.0.1-SNAPSHOT.jar

COPY ${JAR_FILE} mallook-batch.jar

ENTRYPOINT ["java","-Djava.security.egd=file:/dev/./urandom","-jar","/mallook-batch.jar", "--spring.profiles.active=batch"]
