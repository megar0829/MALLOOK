FROM amazoncorretto:17

ARG JAR_FILE=build/libs/mallook-0.0.1-SNAPSHOT.jar

COPY ${JAR_FILE} mallook-sched.jar

ENTRYPOINT ["java","-Djava.security.egd=file:/dev/./urandom","-jar","/mallook-sched.jar", "--spring.profiles.active=sched"]
